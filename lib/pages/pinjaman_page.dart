import 'package:flutter/material.dart';

class PinjamanPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text('Pinjaman')),
      body: Center(child: Text('Halaman Pinjaman Mahasiswa')),
    );
  }
}
